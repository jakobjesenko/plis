chain(
    chain(
        chain(
            chain(chain(memset(0, 9), memset(1, 20)), chain(memset(2, 29), memset(3, 0))),
            chain(chain(memset(4, 0), memset(5, 0)), chain(memset(6, 0), memset(7, 0)))
        ),
        nop()
    ),
    
    
    chain(
        chain(),
        chain()
    ),
)