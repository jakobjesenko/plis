prints(inttostr(proc(5, fac)))
putc(10)
exit(0)
procdef(fac,
    if(<=(arg(0), 1),
        return(1),
        return(
            mul(
                proc(sub(arg(0), 1), fac),
                arg(0)
            )
        )
    )
)